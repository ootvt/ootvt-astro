---
const today = new Date();
---

<footer>
	<div class="footer-content">
		<div class="copyright">© {today.getFullYear()} ootvt</div>
		
		<div class="theme-info" style="font-size: 0.75rem; text-align: center;">
			Powered by <a href="https://astro.build" target="_blank" class="highlight-link">Astro</a>, Theme Memphis by ootvt
		</div>

		<div class="social-links">
			<a href="https://twitter.com/astrodotbuild" target="_blank" aria-label="Twitter">
				<i class="ri-twitter-x-line"></i>
			</a>
			<a href="https://github.com/withastro/astro" target="_blank" aria-label="GitHub">
				<i class="ri-github-line"></i>
			</a>
			<a href="/rss.xml" target="_blank" aria-label="RSS">
				<i class="ri-rss-line"></i>
			</a>
		</div>
	</div>
</footer>

<link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">

<style>
	footer {
		margin-top: 2rem;
		padding: 0rem 0 1.5rem;
		background: transparent;
		width: 100%;
	}

	.footer-content {
		width: 100%;
		max-width: 672px;
		margin: 0 auto;
		padding: 0 1rem;
		display: flex;
		justify-content: space-between;
		align-items: center;
		color: var(--main-color);
		opacity: 0.8;
		box-sizing: border-box;
	}

	.copyright {
		font-size: 0.75rem;
	}

	.theme-info {
		word-break: break-word;
	}

	/* [新增] Astro 链接的样式 */
	.highlight-link {
		color: inherit;           /* 默认继承父元素颜色 */
		text-decoration: none;    /* 去掉下划线 */
		padding: 0 4px;           /* 给左右加点内边距，让方框不要太挤 */
		transition: all 0.2s ease;
		font-weight: bold;        /* 稍微加粗一点，更醒目 */
	}

	/* [新增] 悬停效果：反色方框 */
	.highlight-link:hover {
		background-color: var(--main-color); /* 背景变黑(或主题色) */
		color: var(--bg-color);              /* 文字变白(或背景色) */
		opacity: 1;                          /* 确保不透明 */
		cursor: pointer;
	}

	.social-links {
		display: flex;
		gap: 1rem;
		font-size: 1.125rem;
	}

	.social-links a {
		color: var(--main-color);
		text-decoration: none;
		transition: color 0.2s ease;
		display: flex;
		align-items: center;
	}

	.social-links a:hover {
		color: #000;
	}

	@media (max-width: 768px) {
		.footer-content {
			flex-direction: column;
			gap: 0.5rem;
			text-align: center;
		}
	}
</style>