---
const today = new Date();
---

<footer>
	<div class="footer-content">
		<div class="copyright">© {today.getFullYear()} ootvt</div>
		<div class="social-links">
			<a href="https://twitter.com/astrodotbuild" target="_blank" aria-label="Twitter">
				<i class="ri-twitter-x-line"></i>
			</a>
			<a href="https://github.com/withastro/astro" target="_blank" aria-label="GitHub">
				<i class="ri-github-line"></i>
			</a>
			<a href="/rss.xml" target="_blank" aria-label="RSS">
				<i class="ri-rss-line"></i>
			</a>
		</div>
	</div>
</footer>

<link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">

<style>
	footer {
		margin-top: -0.5rem; /* 保持较小的顶部间距 */
		padding: 1.5rem 1rem;
		background: transparent;
		width: 100%; /* 强制宽度 */
		box-sizing: border-box; /* 防止 padding 撑大导致换行 */
	}

	.footer-content {
		width: 100%; /* 【关键】强制占满容器宽度 */
		max-width: 672px; /* 限制最大宽度与正文一致 */
		margin: 0 auto;
		display: flex;
		justify-content: space-between; /* 【关键】强制两端对齐 */
		align-items: center;
		color: var(--main-color);
		opacity: 0.8;
	}

	.copyright {
		font-size: 0.75rem;
	}

	.social-links {
		display: flex;
		gap: 1rem; /* 图标之间的间距 */
		font-size: 1.125rem;
	}

	.social-links a {
		color: var(--main-color);
		text-decoration: none;
		transition: color 0.2s ease;
		display: flex; /* 防止图标高度塌陷 */
		align-items: center;
	}

	.social-links a:hover {
		color: #000;
	}

	@media (max-width: 768px) {
		.footer-content {
			flex-direction: column; /* 手机端垂直排列 */
			gap: 1rem;
			text-align: center;
		}
	}
</style>