---
const today = new Date();
---

<footer>
	<div class="footer-content">
		<div class="copyright">© {today.getFullYear()} ootvt</div>
		
		<div class="theme-info" style="font-size: 0.75rem; text-align: center;">
			Powered by <a href="https://astro.build" target="_blank" class="highlight-link">Astro</a>, Theme Memphis by ootvt
		</div>

		<div class="social-links">
			<a href="https://twitter.com/astrodotbuild" target="_blank" aria-label="Twitter">
				<i class="ri-twitter-x-line"></i>
			</a>
			<a href="https://github.com/ootvt/ootvt" target="_blank" aria-label="GitHub">
				<i class="ri-github-line"></i>
			</a>
			<a href="/rss.xml" target="_blank" aria-label="RSS">
				<i class="ri-rss-line"></i>
			</a>
		</div>
	</div>
</footer>

<link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">

<style>
	footer {
		margin-top: 2rem;
		padding: 0rem 0 1.5rem;
		background: transparent;
		width: 100%;
	}

	.footer-content {
		width: 100%;
		max-width: 672px;
		margin: 0 auto;
		padding: 0 1rem;
		display: flex;
		justify-content: space-between;
		align-items: center;
		color: var(--main-color);
		opacity: 0.8;
		box-sizing: border-box;
	}

	.copyright {
		font-size: 0.75rem;
	}

	.theme-info {
		word-break: break-word;
	}

	/* [修改] Astro 链接样式 */
	.highlight-link {
		color: inherit;
		text-decoration: none;
        /* 关键：变成行内块级元素，才能撑起背景方块 */
		display: inline-block;
		padding: 0 4px;
        /* 默认背景透明 */
        background-color: transparent;
		transition: all 0.2s ease;
		font-weight: bold;
        /* 确保行高正常，防止方块太高或太扁 */
        line-height: 1.2; 
	}

	/* [修改] 悬停效果：强制变色 */
	.highlight-link:hover {
        /* 使用 !important 确保不被全局样式覆盖 */
		background-color: var(--main-color) !important;
		color: var(--bg-color) !important;
		opacity: 1;
        cursor: pointer;
        /* 可选：加一点微小的圆角，或者保持直角 0px */
        border-radius: 0px; 
	}

	.social-links {
		display: flex;
		gap: 1rem;
		font-size: 1.125rem;
	}

	.social-links a {
		color: var(--main-color);
		text-decoration: none;
		transition: color 0.2s ease;
		display: flex;
		align-items: center;
	}

	.social-links a:hover {
		color: #000;
	}

	@media (max-width: 768px) {
		.footer-content {
			flex-direction: column;
			gap: 0.5rem;
			text-align: center;
		}
	}
</style>